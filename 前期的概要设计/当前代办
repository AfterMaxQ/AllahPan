

## 下一步开发计划推荐

---

### 第一性原理分析

**核心问题**：如何高效推进项目，在最短时间内看到可用效果？

**约束条件**：
1. 后端已完成 80%，但没有前端无法验证
2. PyQt6 是设计文档明确要求的最终形态
3. 项目是本地部署，风险承受能力强
4. 需要快速迭代验证

---

### 推荐方案：分阶段开发

#### 阶段 1：极简 Web 前端验证（1-2天）⭐ 立即开始

**为什么先做 Web 前端？**

| 考量 | 分析 |
|------|------|
| 快速验证 | 1 个 HTML 文件即可测试所有 API |
| 风险分散 | 提前发现后端问题，避免在 PyQt6 开发时返工 |
| 价值验证 | 立即看到用户体验，确认方向正确 |
| 低成本 | 不影响后续 PyQt6 开发，废弃成本极低 |

**极简 Web 前端功能范围**：
- ✅ 登录 / 注册表单
- ✅ 文件列表展示
- ✅ 文件上传（拖拽 + 点击）
- ✅ 文件下载链接
- ✅ AI 语义搜索输入框

**技术实现**：
```
frontend_web/
└── index.html  # 单文件，包含所有 HTML/CSS/JS
```

---

#### 阶段 2：完整 PyQt6 桌面端（6-8天）

**前提条件**：阶段 1 验证通过，后端完全正常

**功能模块规划**：

| 优先级 | 模块 | 说明 |
|--------|------|------|
| P0 | 登录/注册 | 入口页面 |
| P0 | 文件列表 | 核心页面，支持筛选 |
| P0 | 文件上传 | 拖拽上传 + 进度条 |
| P1 | 文件下载 | 右键菜单 + 进度条 |
| P1 | AI 搜索 | 语义搜索结果展示 |
| P2 | 系统设置 | 存储路径、空间监控 |

**项目结构**：
```
frontend_desktop/
├── requirements.txt
├── app/
│   ├── main.py           # 入口
│   ├── app.py            # QApplication
│   ├── pages/
│   │   ├── login_page.py
│   │   ├── files_page.py
│   │   └── settings_page.py
│   ├── widgets/
│   │   ├── file_card.py
│   │   └── upload_dialog.py
│   └── api_client/
│       ├── auth.py
│       └── files.py
└── run.py
```

---

#### 阶段 3：完善 Web 前端（可选）

支持移动端自适应，作为 PyQt6 的补充。

---

### 立即开始的任务

**创建极简 Web 前端**：

1. 创建 `frontend_web/` 目录
2. 创建 `index.html`（单文件，包含登录/注册、文件列表、上传、搜索）
3. 启动后端 `uvicorn app.main:app --reload`
4. 用浏览器打开 `index.html` 测试

---

### 时间估算

| 阶段 | 时间 | 产出 |
|------|------|------|
| 极简 Web 前端 | 1-2 天 | 可运行的 Web 页面 |
| PyQt6 桌面端 | 6-8 天 | 完整原生应用 |
| Web 完善 | 可选 | 移动端支持 |

---

🎨 PyQt6 桌面端设计方案
一、设计理念
核心目标：打造 macOS 风格 + 百度网盘功能模式的精品桌面应用

设计语言：

特性	实现方式
毛玻璃效果	QGraphicsEffect + Alpha 通道模拟
圆角设计	统一 8-12px 圆角
深色模式	系统主题检测 + QSS 切换
精致阴影	多层阴影效果
SF 风格图标	简洁、圆润、线性图标
二、整体布局
┌─────────────────────────────────────────────────────────────┐
│  AllahPan Logo          🔍 搜索栏 (AI/文件名切换)    👤 用户 │
├────────────┬────────────────────────────────────────────────┤
│            │  [上传] [下载] [刷新]     [网格] [列表] [排序]  │
│  📁 全部文件 ├────────────────────────────────────────────────┤
│  🖼️ 图片    │                                                 │
│  📄 文档    │              主内容区域                          │
│  🎬 视频    │          (文件网格/列表视图)                      │
│  🎵 音频    │                                                 │
│  📦 其他    │                                                 │
│  🤖 AI 搜索 │                                                 │
├────────────┴────────────────────────────────────────────────┤
│  💾 存储: 256GB / 512GB    ● Ollama 已连接    ✓ 已同步       │
└─────────────────────────────────────────────────────────────┘
三、配色方案
# 浅色主题
PRIMARY_COLOR = "#007AFF"        # macOS 蓝
SECONDARY_COLOR = "#5856D6"       # 紫色
ACCENT_COLOR = "#34C759"          # 绿色
DANGER_COLOR = "#FF3B30"          # 红色
BACKGROUND = "#FFFFFF"            # 主背景
BACKGROUND_SECONDARY = "#F5F5F7"  # 侧边栏背景
TEXT_PRIMARY = "#1D1D1F"          # 主文字
TEXT_SECONDARY = "#86868B"        # 次级文字
BORDER_COLOR = "#D2D2D7"          # 边框
# 深色主题
DARK_BACKGROUND = "#1C1C1E"
DARK_SURFACE = "#2C2C2E"
DARK_TEXT = "#FFFFFF"
四、核心功能
1. 文件管理
网格视图：大图标 + 文件名，适合图片预览
列表视图：紧凑布局，显示更多文件信息
批量选择：Ctrl/Shift 多选
右键菜单：下载、预览、复制链接
2. 拖拽上传
全局拖拽区：窗口任意位置都可拖入
文件夹支持：递归扫描子文件夹
上传队列：最多 3 个并发，显示进度条
状态反馈：悬停高亮、松开上传
3. AI 语义搜索
搜索模式切换：文件名搜索 / AI 语义搜索
自然语言查询：如"去年的旅行照片"
结果高亮：标注匹配原因

按以下顺序实现：

✅ 项目结构搭建
✅ 主题样式系统（QSS 样式表）
✅ API 客户端封装
✅ UI 组件开发
✅ 主窗口组装

---
**PyQt6 桌面端已完成！**（2026-03-20）

项目已按照设计方案完整实现，包含以下模块：

- config.py：配置常量、主题配色、API地址
- theme/：浅色/深色主题 QSS 样式表
- api/：统一 API 客户端（httpx）
- widgets/：侧边栏、搜索栏、上传队列、状态栏、文件浏览器
- pages/：登录页、设置页
- main_window.py：主窗口整合
- run.py：入口脚本

详见：`frontend_desktop/README.md`

---

下一步建议：
1. 安装依赖：`pip install -r frontend_desktop/requirements.txt`
2. 确保后端运行：`cd backend && uvicorn app.main:app --reload`
3. 启动桌面端：`python frontend_desktop/run.py`