# Python
.venv/
venv/
env/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
*.egg

# IDE / Editor
.idea/
.vscode/
*.swp
*.swo

# PyInstaller 中间产物备份
*.spec.bak

# dist/：Windows 打包目录内含 Qt WebEngine 等单文件可超过 GitHub 100MB 硬限制，无法推送。
# 换机后请在本机执行：pip install -r requirements.txt && pip install PyInstaller && pyinstaller AllahPan.spec
dist/

# build/：纳入仓库便于保留 spec 构建辅助文件、版本信息等（体积相对可控）。
# 若某文件仍超 100MB，再改回忽略或改用 Git LFS。

# Runtime / Data
data/
*.db
*.sqlite
*.log
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Trae (if present)
.trae/

# 前期设计文档（本地工作文件，不纳入版本控制）
前期的概要设计/

# Optional: keep spec files tracked (AllahPan.spec, AllahPan-macOS.spec, AllahPan-Linux.spec)
# Do not ignore *.spec so packaging config is in repo
