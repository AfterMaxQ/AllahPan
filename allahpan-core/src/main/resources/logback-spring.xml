<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty scope="context" name="serviceName" source="allahpan.logging.service-name" defaultValue="allahpan-core"/>
    <springProperty scope="context" name="profileName" source="spring.profiles.active" defaultValue="dev"/>
    <springProperty scope="context" name="logPath" source="logging.file.path" defaultValue="./logs"/>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder><pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level service=${serviceName} profile=${profileName} requestId=%X{requestId:-} operationId=%X{operationId:-} jobId=%X{jobId:-} userId=%X{userId:-} %msg%n%ex{full}</pattern></encoder>
    </appender>
    <springProfile name="prod | docker">
    <appender name="ROLLING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${logPath}/allahpan-core.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${logPath}/allahpan-core.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>100MB</maxFileSize><maxHistory>30</maxHistory><totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
        <encoder><pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level service=${serviceName} profile=${profileName} requestId=%X{requestId:-} operationId=%X{operationId:-} jobId=%X{jobId:-} userId=%X{userId:-} %msg%n%ex{full}</pattern></encoder>
    </appender>
    </springProfile>
    <springProfile name="prod | docker">
        <root level="WARN"><appender-ref ref="CONSOLE"/><appender-ref ref="ROLLING_FILE"/></root>
        <logger name="com.allahpan" level="INFO"/>
    </springProfile>
    <springProfile name="dev &amp; !docker">
        <root level="INFO"><appender-ref ref="CONSOLE"/></root>
        <logger name="com.allahpan" level="DEBUG"/>
    </springProfile>
</configuration>
